package org.example.strings;

/**
 * Задача: Подсчёт вхождений подстроки
 * <p>
 * Условие:
 * Напиши метод, который возвращает количество непересекающихся вхождений
 * подстроки `subStr` в строку `str`.
 * Примеры:
 * ("ababab", "ab") → 3
 * ("aaaaa", "aa") → 2
 * ("abc", "d") → 0
 * <p>
 */
public class SubstringCount {

    // TODO: реализуй метод
    public static int countSubstring(String str, String subStr) {
        return 0; // реализация метода
    }

    public static void main(String[] args) {
        // Примеры входных данных:
        System.out.println(countSubstring("ababa", "ab")); //2
    }
}