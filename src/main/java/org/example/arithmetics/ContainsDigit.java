package org.example.arithmetics;

public class ContainsDigit {

    /**
     * Задача: Содержит ли число заданную цифру
     *
     * Условие:
     * Напиши метод, который принимает два числа: `n` и `digit`,
     * и возвращает true, если `digit` встречается в числе `n`.
     *
     * Примеры:
     * containsDigit(12345, 3) → true
     * containsDigit(9876, 1)  → false
     * containsDigit(0, 0)     → true
     */

    // TODO: реализуй метод ниже
    public static boolean containsDigit(int n, int digit) {
        // реализация здесь
        return false;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(containsDigit(12345, 3)); // true
        System.out.println(containsDigit(9876, 1));  // false
        System.out.println(containsDigit(0, 0));     // true
    }
}
