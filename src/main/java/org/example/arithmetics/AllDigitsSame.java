package org.example.arithmetics;

public class AllDigitsSame {

    /**
     * Задача: Все ли цифры одинаковые
     *
     * Условие:
     * Напиши метод, который проверяет, состоят ли все цифры в числе `n` из одного и того же значения.
     *
     * Примеры:
     * allDigitsSame(1111) → true
     * allDigitsSame(222)  → true
     * allDigitsSame(123)  → false
     * allDigitsSame(0)    → true
     */

    // TODO: реализуй метод ниже
    public static boolean allDigitsSame(int n) {
        // реализация здесь
        return false;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(allDigitsSame(1111)); // true
        System.out.println(allDigitsSame(222));  // true
        System.out.println(allDigitsSame(123));  // false
        System.out.println(allDigitsSame(0));    // true
    }
}
