package org.example.arrays;

public class Average {

    /**
     * Задача: Найти среднее арифметическое элементов массива
     *
     * Условие:
     * Напиши метод, который принимает массив целых чисел
     * и возвращает их среднее арифметическое как число с плавающей точкой (double).
     *
     * Примеры:
     * average(new int[]{1, 2, 3, 4}) → 2.5
     * average(new int[]{5, 15})     → 10.0
     */

    // TODO: реализуй метод ниже
    public static double average(int[] arr) {
        // реализация здесь
        return 0.0;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(average(new int[]{1, 2, 3, 4})); // 2.5
        System.out.println(average(new int[]{5, 15}));      // 10.0
    }
}
