package org.example.arrays;

public class IsSortedDescending {

    /**
     * Задача: Проверить, отсортирован ли массив по убыванию
     *
     * Условие:
     * Напиши метод, который возвращает true, если массив отсортирован строго по убыванию.
     *
     * Примеры:
     * isSortedDescending(new int[]{9, 7, 5, 3}) → true
     * isSortedDescending(new int[]{9, 7, 7, 3}) → false
     * isSortedDescending(new int[]{1, 2, 3})    → false
     */

    // TODO: реализуй метод ниже
    public static boolean isSortedDescending(int[] arr) {
        // реализация здесь
        return false;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(isSortedDescending(new int[]{9, 7, 5, 3})); // true
        System.out.println(isSortedDescending(new int[]{9, 7, 7, 3})); // false
        System.out.println(isSortedDescending(new int[]{1, 2, 3}));    // false
    }
}
