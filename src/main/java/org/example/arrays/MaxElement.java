package org.example.arrays;

public class MaxElement {

    /**
     * Задача: Найти максимальный элемент массива
     *
     * Условие:
     * Напиши метод, который принимает массив целых чисел
     * и возвращает максимальный элемент в этом массиве.
     *
     * Примеры:
     * maxElement(new int[]{3, 5, 1, 8, 2}) → 8
     * maxElement(new int[]{7, 4, 9})       → 9
     */

    // TODO: реализуй метод ниже
    public static int maxElement(int[] arr) {
        // реализация здесь
        return 0;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(maxElement(new int[]{3, 5, 1, 8, 2})); // 8
        System.out.println(maxElement(new int[]{7, 4, 9}));       // 9
    }
}
