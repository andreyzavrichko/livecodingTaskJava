package org.example.arrays;

public class MinElement {

    /**
     * Задача: Найти минимальный элемент массива
     *
     * Условие:
     * Напиши метод, который принимает массив целых чисел
     * и возвращает минимальный элемент в этом массиве.
     *
     * Примеры:
     * minElement(new int[]{3, 5, 1, 8, 2}) → 1
     * minElement(new int[]{7, 4, 9})       → 4
     */

    // TODO: реализуй метод ниже
    public static int minElement(int[] arr) {
        // реализация здесь
        return 0;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(minElement(new int[]{3, 5, 1, 8, 2})); // 1
        System.out.println(minElement(new int[]{7, 4, 9}));       // 4
    }
}
