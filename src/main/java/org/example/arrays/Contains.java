package org.example.arrays;

public class Contains {

    /**
     * Задача: Проверка наличия элемента в массиве
     *
     * Условие:
     * Напиши метод, который принимает массив целых чисел и целевое значение,
     * и возвращает true, если значение присутствует в массиве.
     *
     * Примеры:
     * contains(new int[]{1, 2, 3}, 2) → true
     * contains(new int[]{4, 5, 6}, 7) → false
     */

    // TODO: реализуй метод ниже
    public static boolean contains(int[] arr, int target) {
        // реализация здесь
        return false;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(contains(new int[]{1, 2, 3}, 2)); // true
        System.out.println(contains(new int[]{4, 5, 6}, 7)); // false
    }
}
