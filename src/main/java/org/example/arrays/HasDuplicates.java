package org.example.arrays;

public class HasDuplicates {

    /**
     * Задача: Проверить, содержит ли массив дубликаты
     * <p>
     * Условие:
     * Напиши метод, который возвращает true, если в массиве есть хотя бы одно повторяющееся значение.
     * <p>
     * Примеры:
     * hasDuplicates(new int[]{1, 2, 3, 4}) → false
     * hasDuplicates(new int[]{1, 2, 3, 2}) → true
     */

    // TODO: реализуй метод ниже
    public static boolean hasDuplicates(int[] arr) {
        // реализация здесь
        return false;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(hasDuplicates(new int[]{1, 2, 3, 4})); // false
        System.out.println(hasDuplicates(new int[]{1, 2, 3, 2})); // true
    }
}
