package org.example.arrays;

public class CountUnique {

    /**
     * Задача: Подсчёт количества уникальных элементов
     * <p>
     * Условие:
     * Напиши метод, который принимает массив целых чисел
     * и возвращает количество уникальных значений (без повторений).
     * <p>
     * Примеры:
     * countUnique(new int[]{1, 2, 2, 3, 4, 4}) → 4
     * countUnique(new int[]{5, 5, 5})         → 1
     */

    // TODO: реализуй метод ниже
    public static int countUnique(int[] arr) {
        // реализация здесь
        return 0;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(countUnique(new int[]{1, 2, 2, 3, 4, 4})); // 4
        System.out.println(countUnique(new int[]{5, 5, 5}));         // 1
    }
}
