package org.example.arrays;

public class IndexOf {

    /**
     * Задача: Найти индекс первого вхождения элемента
     *
     * Условие:
     * Напиши метод, который принимает массив целых чисел и целевое значение,
     * и возвращает индекс первого вхождения этого значения в массиве.
     * Если значение не найдено — верни -1.
     *
     * Примеры:
     * indexOf(new int[]{1, 3, 5, 3}, 3) → 1
     * indexOf(new int[]{1, 2, 3}, 4)    → -1
     */

    // TODO: реализуй метод ниже
    public static int indexOf(int[] arr, int target) {
        // реализация здесь
        return -1;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(indexOf(new int[]{1, 3, 5, 3}, 3)); // 1
        System.out.println(indexOf(new int[]{1, 2, 3}, 4));    // -1
    }
}
