package org.example.arrays;

public class MaxDiffBetweenNeighbors {

    /**
     * Задача: Найти наибольшую разницу между двумя соседними элементами
     *
     * Условие:
     * Напиши метод, который находит и возвращает наибольшую разницу между любыми двумя
     * соседними элементами массива.
     *
     * Примеры:
     * maxDiffBetweenNeighbors(new int[]{1, 5, 2, 10}) → 8  // |10 - 2|
     * maxDiffBetweenNeighbors(new int[]{4, 4, 4})     → 0
     */

    // TODO: реализуй метод ниже
    public static int maxDiffBetweenNeighbors(int[] arr) {
        // реализация здесь
        return 0;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(maxDiffBetweenNeighbors(new int[]{1, 5, 2, 10})); // 8
        System.out.println(maxDiffBetweenNeighbors(new int[]{4, 4, 4}));     // 0
    }
}
