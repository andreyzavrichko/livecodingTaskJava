package org.example.arrays;

public class CountEvens {

    /**
     * Задача: Подсчёт чётных чисел в массиве
     *
     * Условие:
     * Напиши метод, который принимает массив целых чисел
     * и возвращает количество чётных чисел в нём.
     *
     * Примеры:
     * countEvens(new int[]{3, 5, 2, 8, 1}) → 2
     * countEvens(new int[]{1, 3, 5})      → 0
     */

    // TODO: реализуй метод ниже
    public static int countEvens(int[] arr) {
        // реализация здесь
        return 0;
    }

    public static void main(String[] args) {
        // Примеры для запуска
        System.out.println(countEvens(new int[]{3, 5, 2, 8, 1})); // 2
        System.out.println(countEvens(new int[]{1, 3, 5}));       // 0
    }
}
